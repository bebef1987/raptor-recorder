# raptor-recorder

Usage:
```
usage: recorder.py [-h] [--recording RECORDING]

Start and record a mitmProxy recording

optional arguments:
  -h, --help            show this help message and exit
  --recording RECORDING
                        recording name
```
